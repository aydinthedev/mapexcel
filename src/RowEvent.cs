namespace MapExcel;

public enum RowEvent
{
    /// <summary>
    ///     When the row is read from the worksheet
    /// </summary>
    Read,

    /// <summary>
    ///     When the row is written to the worksheet
    /// </summary>
    Write
}